spring:
  datasource:
    url: jdbc:h2:tcp://10.50.1.105:1521/demo
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: prod

server:
  port: 8080
  ssl:
    enabled: false